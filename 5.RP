/*Write a program to implement the following
a. Implicit type declaration
b. Explicit type declaration*/
fn main() {
    let name = "Rust";
    let age = 25;
    let pi = 3.14;
    println!("Name: {}, Age: {}, Pi: {}", name, age, pi);
    let language: &str = "Rust Programming";
    let year: u32 = 2025;
    let version: f32 = 1.75;
    println!("Language: {}, Year: {}, Version: {}", language, year, version);
}
